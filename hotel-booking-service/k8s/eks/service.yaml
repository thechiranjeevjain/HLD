apiVersion: v1
kind: Service
metadata:
  name: booking-api
  namespace: booking
spec:
  type: ClusterIP
  selector:
    app: booking-api
  ports:
    - name: http
      port: 80
      targetPort: http
