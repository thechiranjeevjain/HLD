apiVersion: v1
kind: Service
metadata:
  name: exchange-lite-sidecar
  namespace: exchange-lite
spec:
  selector:
    app: exchange-lite
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: data
      port: 9090
      targetPort: 9090
  type: ClusterIP
